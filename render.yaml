services:
  - type: cron
    name: apt-listing-checker
    schedule: "0 */3 * * *"  # Every 3 hours (at minute 0)
    env: python
    plan: free
    buildCommand: ""
    startCommand: "python apt_scraper.py"
